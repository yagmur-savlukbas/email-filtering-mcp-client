{
  "name": "email-reminder-mcp",
  "version": "1.0.0",
  "description": "MCP server for filtering important emails and avoiding spam",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run copy-credentials",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build",
    "copy-credentials": "node -e \"const fs=require('fs'); const path=require('path'); if(fs.existsSync('src/credentials.json')){fs.copyFileSync('src/credentials.json','dist/credentials.json'); console.log('✓ Copied credentials.json to dist/');}\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "googleapis": "^128.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
